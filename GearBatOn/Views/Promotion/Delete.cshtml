@model GearBatOn.Models.Promotion

@{
    ViewBag.Title = "Xóa khuyến mãi";
    Layout = "~/Views/Shared/_StaffLayout.cshtml";
}

<h2>Xóa khuyến mãi</h2>

<h3>Bạn có chắc muốn xóa khuyến mãi này không?</h3>
<div>
    <h4>Khuyến mãi</h4>
    <hr />

    <dl class="row">
        <dt class="col-sm-3">Mã</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Id)</dd>

        <dt class="col-sm-3">Tên khuyến mãi</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

        <dt class="col-sm-3">Mã khuyến mãi</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.PromoCode)</dd>

        <dt class="col-sm-3">Tỷ lệ</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Ratio)</dd>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Xóa" class="btn btn-danger" /> 
            @Html.ActionLink("Quay trở lại danh sách", "Index", routeValues: null, htmlAttributes: new { @class = "btn btn-primary", type = "button" })
        </div>
    }
</div>
